spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/dormitory_test?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 123456
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwt:
    secret: dormitory-jwt-secret-change-me-please-32bytes-min-000000
    expiration-seconds: 7200

logging:
  level:
    root: warn
